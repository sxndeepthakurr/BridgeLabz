class Solution {
    public int lengthOfLastWord(String s) {
        if(s==null || s.isEmpty()){
            return 0;
        }
        s=s.trim(); //remove the trailing spaces
        int lastspaceindex=s.lastIndexOf(' ');
        return s.length()-lastspaceindex-1;  
    }
}